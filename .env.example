# Copy this to `.env` and fill in values for local development
EMAIL_USER=
EMAIL_PASS=
# optional SMTP overrides
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
# PORT to run the dev server (default 3000)
PORT=3000